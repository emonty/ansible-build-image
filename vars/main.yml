---
# vars file for infra/build-image
distro: ubuntu
release: xenial
elements_path:
- /opt/project-config/nodepool/elements
image_outdir: /var/lib/dib-images
image_tmpdir: /tmp
qemu_image_options: compat=0.10
formats:
- qcow2
